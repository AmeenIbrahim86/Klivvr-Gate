-- ══════════════════════════════════════════════════════════════
--  علامة على أي بروفايل تم إنشاؤه كحساب محلي (إيميل/باسورد) بدل
--  Microsoft — عشان نعرف مين نقدر نغيّر باسورده أو نمسحه بالكامل
-- ══════════════════════════════════════════════════════════════

alter table profiles add column if not exists is_local boolean default false;
