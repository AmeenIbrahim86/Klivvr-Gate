-- ══════════════════════════════════════════════════════════════
--  ربط كل قاعة اجتماعات بفرع معيّن — عشان تقدر تضيف/تمسح/تعدّل
--  قاعات بالكامل من الإدارة، مش بس تعدّل الإيميل
-- ══════════════════════════════════════════════════════════════

alter table meeting_rooms add column if not exists branch_id text references branches(id);

notify pgrst, 'reload schema';
