-- ══════════════════════════════════════════════════════════════
--  ربط كل قاعة اجتماعات بفرع، ودعم إضافة/حذف/تعديل كامل بدل
--  تعديل الإيميل بس
-- ══════════════════════════════════════════════════════════════

alter table meeting_rooms add column if not exists branch_id text references branches(id);
