-- ══════════════════════════════════════════════════════════════
--  خيار رابط (URL) اختياري لأي مقالة في قاعدة المعرفة — بدل النص
--  الداخلي أو معاه، ممكن يكون رابط لصفحة/مستند خارجي
-- ══════════════════════════════════════════════════════════════

alter table kb_articles add column if not exists url text;

notify pgrst, 'reload schema';
